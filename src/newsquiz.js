(function () {
    'use strict';

    var NewsQuiz = function (selector, questions) {
        this.selector = document.querySelector(selector);
        this.questions = questions;
    };

}).call(this);